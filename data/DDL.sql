CREATE TABLE imdb_data (
    ID int NOT NULL AUTO_INCREMENT,
    Year varchar(255),
    Length varchar(255),
    Title varchar(255),
    Subject varchar(255),
    Popularity varchar(255),
    Awards varchar(255)
);